# Experiment 3 — Theme Toggle SPA

This is a minimal React + Vite single-page app demonstrating a light/dark theme toggle using React state and CSS variables.

Quick start:

```bash
npm install
npm run dev
```
